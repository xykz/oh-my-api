# lingma2api v1.1 精准化实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 上游返回真实 usage 时优先使用，否则 fallback 字符/4；删除前端旧 Models 兼容映射页面及后端 `/admin/mappings` API。

**Architecture：** Handler 层在解析上游 SSE 响应时提取 usage 字段，通过新增参数传递给 `persistCanonicalExecutionRecord`；canonical metrics 读取时优先用真实值。前端删除 Models 页面，路由重定向到 Policies。

**Tech Stack：** Go 1.24, React/TS, SQLite, standard library

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `internal/api/canonical_runtime.go` | 修改 `persistCanonicalExecutionRecord`，接受 token 计数参数 |
| `internal/api/server.go` | OpenAI handler 提取 usage；删除 `/admin/mappings` 路由 |
| `internal/api/anthropic_handler.go` | Anthropic handler 提取 usage |
| `internal/db/canonical_metrics.go` | `CanonicalRecordTokenCounts` 优先读取真实值 |
| `frontend/src/App.tsx` | 删除 `/models` 路由，添加重定向 |
| `frontend/src/pages/Models.tsx` | 删除文件 |
| `internal/api/admin_handlers.go` | 删除 `/admin/mappings` 相关 handler 和辅助函数 |
| `internal/api/admin_policies_test.go` | 删除 `/admin/mappings` 相关测试 |

---

### Task 1: 修改 `persistCanonicalExecutionRecord` 支持传入真实 token 计数

**Files:**
- Modify: `internal/api/canonical_runtime.go`
- Modify: `internal/api/canonical_runtime_test.go`（如有）

**Context:** 当前 `persistCanonicalExecutionRecord` 在内部用 `db.EstimateMessageTokens` 计算 token。需要改为：调用方传入真实 token 计数，函数内部有值则用，无值则 fallback 到估算。

- [ ] **Step 1: 修改函数签名和实现**

在 `internal/api/canonical_runtime.go` 中，修改 `persistCanonicalExecutionRecord`：

```go
func (server *Server) persistCanonicalExecutionRecord(
	ctx context.Context,
	ingressProtocol proxy.CanonicalProtocol,
	ingressEndpoint string,
	prePolicyRequest proxy.CanonicalRequest,
	postPolicyRequest proxy.CanonicalRequest,
	sessionCanonicalRequest proxy.CanonicalRequest,
	projectedRequest proxy.OpenAIChatRequest,
	requestMessages []proxy.Message,
	assistant proxy.Message,
	remoteRequest proxy.RemoteChatRequest,
	rawSSELines []string,
	promptTokens int,
	completionTokens int,
	totalTokens int,
) {
	if server.db == nil {
		return
	}

	// Fallback to estimation if no real counts provided
	if promptTokens <= 0 {
		promptTokens = db.EstimateMessageTokens(requestMessages)
	}
	if completionTokens <= 0 {
		completionTokens = db.EstimateMessageTokens([]proxy.Message{assistant})
	}
	if totalTokens <= 0 {
		totalTokens = promptTokens + completionTokens
	}

	record := &db.CanonicalExecutionRecordRow{
		ID:                proxy.NewUUID(),
		CreatedAt:         server.deps.Now(),
		IngressProtocol:   string(ingressProtocol),
		IngressEndpoint:   ingressEndpoint,
		SessionID:         postPolicyRequest.SessionID,
		PrePolicyRequest:  prePolicyRequest,
		PostPolicyRequest: postPolicyRequest,
		SessionSnapshot: buildCanonicalSessionSnapshot(
			ingressProtocol,
			sessionCanonicalRequest,
			assistant,
			server.deps.Now(),
		),
		SouthboundRequest: remoteRequest.BodyJSON,
		Sidecar: &proxy.CanonicalExecutionSidecar{
			SchemaVersion: 1,
			RawSSELines:   rawSSELines,
			Metadata: map[string]any{
				"request_id":        remoteRequest.RequestID,
				"model_key":         remoteRequest.ModelKey,
				"stream":            remoteRequest.Stream,
				"upstream_status":   200,
				"prompt_tokens":     promptTokens,
				"completion_tokens": completionTokens,
				"total_tokens":      totalTokens,
			},
		},
	}
	_ = server.db.InsertCanonicalExecutionRecord(ctx, record)
}
```

- [ ] **Step 2: 更新所有调用点（临时传入 0，后续 Task 再填真实值）**

目前调用点有 4 处：
1. `internal/api/server.go:361` — `writeNonStreamResponse`
2. `internal/api/server.go:555` — `streamChatResponse`
3. `internal/api/anthropic_handler.go:210` — `nonStreamAnthropicResponse`
4. `internal/api/anthropic_handler.go:436` — `streamAnthropicResponse`

先全部在调用时末尾加三个 `0`：

```go
server.persistCanonicalExecutionRecord(
    ctx,
    prePolicyRequest.Protocol,
    "/v1/chat/completions",
    prePolicyRequest,
    postPolicyRequest,
    sessionCanonicalRequest,
    request,
    messages,
    assistant,
    remoteRequest,
    rawSSELines,
    0, 0, 0,  // real token counts (extracted in later tasks)
)
```

- [ ] **Step 3: 编译确认**

Run: `go build ./...`
Expected: 编译通过（所有调用点参数已补齐）

- [ ] **Step 4: Commit**

```bash
git add internal/api/canonical_runtime.go internal/api/server.go internal/api/anthropic_handler.go
git commit -m "feat(api): add token count params to persistCanonicalExecutionRecord

- accept prompt/completion/total tokens from caller
- fallback to EstimateMessageTokens when real counts are 0
- all call sites temporarily pass 0

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 2: OpenAI handler 提取上游 usage

**Files:**
- Modify: `internal/api/server.go`

**Context:** OpenAI 协议的非流式和流式响应都可能带 usage。上游返回的是 SSE 格式，需要解析。

对于非流式：`proxy.CollectSSEContentWithLines` 收集内容但不解析 usage。需要自定义解析逻辑。
对于流式：SSE 的最后一个 chunk 可能带 `usage`。

- [ ] **Step 1: 非流式响应提取 usage**

在 `writeNonStreamResponse` 中，替换 `proxy.CollectSSEContentWithLines` 的调用，改为同时提取 usage：

```go
func (server *Server) writeNonStreamResponse(
	ctx context.Context,
	writer http.ResponseWriter,
	request proxy.OpenAIChatRequest,
	remoteRequest proxy.RemoteChatRequest,
	sessionID string,
	messages []proxy.Message,
	stream io.Reader,
	prePolicyRequest proxy.CanonicalRequest,
	postPolicyRequest proxy.CanonicalRequest,
	sessionCanonicalRequest proxy.CanonicalRequest,
) {
	content, rawSSELines, promptTokens, completionTokens, totalTokens, err := collectSSEContentWithUsage(stream)
	if err != nil {
		writeMappedError(writer, err)
		return
	}
	assistant := proxy.Message{
		Role:    "assistant",
		Content: content,
	}
	if err := server.deps.Sessions.SaveCanonicalResponse(context.Background(), sessionID, sessionCanonicalRequest, assistant); err != nil {
		writeMappedError(writer, err)
		return
	}
	server.persistCanonicalExecutionRecord(
		ctx,
		prePolicyRequest.Protocol,
		"/v1/chat/completions",
		prePolicyRequest,
		postPolicyRequest,
		sessionCanonicalRequest,
		request,
		messages,
		assistant,
		remoteRequest,
		rawSSELines,
		promptTokens,
		completionTokens,
		totalTokens,
	)

	finishReason := "stop"
	writeJSON(writer, http.StatusOK, chatCompletionResponse{
		ID:      "chatcmpl-" + remoteRequest.RequestID,
		Object:  "chat.completion",
		Created: server.deps.Now().Unix(),
		Model:   request.Model,
		Choices: []chatCompletionChoice{
			{
				Index: 0,
				Message: &proxy.Message{
					Role:    "assistant",
					Content: content,
				},
				FinishReason: &finishReason,
			},
		},
		Usage: &openAIUsage{  // 添加 usage 到响应
			PromptTokens:     promptTokens,
			CompletionTokens: completionTokens,
			TotalTokens:      totalTokens,
		},
	})
}
```

- [ ] **Step 2: 添加 `collectSSEContentWithUsage` 辅助函数**

在 `internal/api/server.go` 中（放在 `writeNonStreamResponse` 附近）：

```go
func collectSSEContentWithUsage(reader io.Reader) (content string, rawLines []string, promptTokens, completionTokens, totalTokens int, err error) {
	var builder strings.Builder
	err = proxy.ScanSSEWithLines(reader, func(line string) error {
		rawLines = append(rawLines, line)
		return nil
	}, func(event proxy.SSEEvent) error {
		if event.Usage != nil {
			promptTokens = event.Usage.PromptTokens
			completionTokens = event.Usage.CompletionTokens
			totalTokens = event.Usage.TotalTokens
		}
		builder.WriteString(event.Content)
		return nil
	})
	if err != nil {
		return "", nil, 0, 0, 0, err
	}
	if rawLines == nil {
		rawLines = []string{}
	}
	return builder.String(), rawLines, promptTokens, completionTokens, totalTokens, nil
}
```

**注意：** 这要求 `proxy.SSEEvent` 有 `Usage` 字段。检查 `internal/proxy/sse.go` 中 `SSEEvent` 是否已有 `Usage` 字段。如果没有，需要先添加：

```go
type SSEEvent struct {
	Content          string
	ReasoningContent string
	ToolCalls        []proxy.ToolCall
	Done             bool
	Usage            *openAIUsage  // 新增
}
```

但实际上 `SSEEvent` 在 `proxy` 包中，不应该依赖 `api` 包的类型。更好的做法是在 `proxy` 包中定义：

```go
type SSEUsage struct {
	PromptTokens     int
	CompletionTokens int
	TotalTokens      int
}

type SSEEvent struct {
	Content          string
	ReasoningContent string
	ToolCalls        []ToolCall
	Done             bool
	Usage            *SSEUsage
}
```

这需要修改 `internal/proxy/sse.go` 中的 `SSEEvent` 定义，以及在解析 SSE 时填充 Usage。

- [ ] **Step 3: 修改 `proxy.SSEEvent` 添加 Usage 字段**

在 `internal/proxy/sse.go` 中：

```go
type SSEUsage struct {
	PromptTokens     int
	CompletionTokens int
	TotalTokens      int
}

type SSEEvent struct {
	Content          string
	ReasoningContent string
	ToolCalls        []ToolCall
	Done             bool
	Usage            *SSEUsage
}
```

在 SSE 解析逻辑中（`parseSSEvent` 或类似函数），当解析到 `usage` 字段时填充：

```go
if usageRaw, ok := body["usage"]; ok && usageRaw != nil {
    if usageMap, ok := usageRaw.(map[string]any); ok {
        event.Usage = &SSEUsage{}
        if v, ok := usageMap["prompt_tokens"].(float64); ok {
            event.Usage.PromptTokens = int(v)
        }
        if v, ok := usageMap["completion_tokens"].(float64); ok {
            event.Usage.CompletionTokens = int(v)
        }
        if v, ok := usageMap["total_tokens"].(float64); ok {
            event.Usage.TotalTokens = int(v)
        }
    }
}
```

- [ ] **Step 4: 流式响应提取 usage**

在 `streamChatResponse` 中，SSE 扫描回调里已经能访问 `event.Usage`。在函数末尾调用 `persistCanonicalExecutionRecord` 之前累加 usage：

```go
func (server *Server) streamChatResponse(...) {
	// ... existing setup ...
	var contentBuilder strings.Builder
	var rawSSELines []string
	var promptTokens, completionTokens, totalTokens int

	err := proxy.ScanSSEWithLines(stream, func(line string) error {
		rawSSELines = append(rawSSELines, line)
		return nil
	}, func(event proxy.SSEEvent) error {
		if event.Usage != nil {
			promptTokens = event.Usage.PromptTokens
			completionTokens = event.Usage.CompletionTokens
			totalTokens = event.Usage.TotalTokens
		}
		// ... rest of event handling ...
	})
	// ... error handling ...

	assistant := proxy.Message{...}
	if err := server.deps.Sessions.SaveCanonicalResponse(...); err != nil {
		return
	}
	server.persistCanonicalExecutionRecord(
		request.Context(),
		prePolicyRequest.Protocol,
		"/v1/chat/completions",
		prePolicyRequest,
		postPolicyRequest,
		sessionCanonicalRequest,
		chatRequest,
		messages,
		assistant,
		remoteRequest,
		rawSSELines,
		promptTokens,
		completionTokens,
		totalTokens,
	)
	// ... rest ...
}
```

- [ ] **Step 5: 编译确认**

Run: `go build ./...`
Expected: 编译通过

- [ ] **Step 6: Commit**

```bash
git add internal/proxy/sse.go internal/api/server.go
git commit -m "feat(api): extract upstream usage in OpenAI handler

- add SSEUsage to proxy.SSEEvent
- collectSSEContentWithUsage helper for non-streaming
- streamChatResponse accumulates usage from SSE events
- pass real token counts to persistCanonicalExecutionRecord

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 3: Anthropic handler 提取上游 usage

**Files:**
- Modify: `internal/api/anthropic_handler.go`

**Context:** Anthropic 协议的上游也是 SSE。`message_start` 事件可能带 `usage`，`message_delta` 也可能带。需要解析并提取。

**注意：** Anthropic 的 usage 字段名是 `input_tokens` 和 `output_tokens`，与 OpenAI 不同。

- [ ] **Step 1: 非流式响应提取 usage**

在 `nonStreamAnthropicResponse` 中，修改 SSE 扫描回调，提取 usage：

```go
func (server *Server) nonStreamAnthropicResponse(...) {
	var contentBuilder strings.Builder
	var reasoningBuilder strings.Builder
	var rawSSELines []string
	var inputTokens, outputTokens int

	err := proxy.ScanSSEWithLines(stream, func(line string) error {
		rawSSELines = append(rawSSELines, line)
		return nil
	}, func(event proxy.SSEEvent) error {
		// Check for usage in the SSE event
		if event.Usage != nil {
			inputTokens = event.Usage.PromptTokens
			outputTokens = event.Usage.CompletionTokens
		}
		contentBuilder.WriteString(event.Content)
		reasoningBuilder.WriteString(event.ReasoningContent)
		return nil
	})
	// ... error handling ...

	// Use real tokens if available, otherwise fallback
	promptTokens := inputTokens
	completionTokens := outputTokens
	totalTokens := inputTokens + outputTokens
	if promptTokens == 0 && completionTokens == 0 {
		// fallback to chars/4
		promptTokens = len(contentText)/4 + len(reasoningText)/4
		completionTokens = len(contentText)/4 + len(reasoningText)/4
		totalTokens = promptTokens + completionTokens
	}

	usage := proxy.Usage{
		InputTokens:  promptTokens,
		OutputTokens: completionTokens,
	}
	// ... build resp ...

	server.persistCanonicalExecutionRecord(
		ctx,
		prePolicyRequest.Protocol,
		"/v1/messages",
		prePolicyRequest,
		postPolicyRequest,
		sessionCanonicalRequest,
		request,
		messages,
		assistant,
		remoteRequest,
		rawSSELines,
		promptTokens,
		completionTokens,
		totalTokens,
	)
	// ...
}
```

- [ ] **Step 2: 流式响应提取 usage**

在 `streamAnthropicResponse` 中，修改 SSE 扫描回调：

```go
func (server *Server) streamAnthropicResponse(...) {
	// ... setup ...
	var contentBuilder strings.Builder
	var reasoningBuilder strings.Builder
	var rawSSELines []string
	var inputTokens, outputTokens int

	err := proxy.ScanSSEWithLines(stream, func(line string) error {
		rawSSELines = append(rawSSELines, line)
		return nil
	}, func(event proxy.SSEEvent) error {
		if event.Usage != nil {
			inputTokens = event.Usage.PromptTokens
			outputTokens = event.Usage.CompletionTokens
		}
		// ... existing content handling ...
	})
	// ... error handling and closeBlock ...

	totalText := contentBuilder.String()
	totalReasoning := reasoningBuilder.String()

	// Use real tokens if available
	promptTokens := inputTokens
	completionTokens := outputTokens
	outTokens := completionTokens
	if outTokens == 0 {
		outTokens = (len(totalText) + len(totalReasoning)) / 4
		if outTokens == 0 {
			outTokens = 1
		}
		completionTokens = outTokens
	}
	if promptTokens == 0 {
		promptTokens = outTokens * 2  // fallback
	}
	totalTokens := promptTokens + completionTokens

	// message_delta 中的 usage
	writeAnthropicSSE(writer, proxy.AnthropicStreamEvent{
		Type: "message_delta",
		Delta: &proxy.StreamDelta{
			StopReason: "end_turn",
		},
		Usage: &proxy.Usage{
			OutputTokens: completionTokens,
			InputTokens:  promptTokens,
		},
	})
	// ... rest ...

	server.persistCanonicalExecutionRecord(
		request.Context(),
		prePolicyRequest.Protocol,
		"/v1/messages",
		prePolicyRequest,
		postPolicyRequest,
		sessionCanonicalRequest,
		chatRequest,
		messages,
		assistant,
		remoteRequest,
		rawSSELines,
		promptTokens,
		completionTokens,
		totalTokens,
	)
}
```

- [ ] **Step 3: 编译确认**

Run: `go build ./...`
Expected: 编译通过

- [ ] **Step 4: Commit**

```bash
git add internal/api/anthropic_handler.go
git commit -m "feat(api): extract upstream usage in Anthropic handler

- non-streaming: extract usage from SSE events, fallback to chars/4
- streaming: extract usage from SSE events, fallback to chars/4
- pass real token counts to persistCanonicalExecutionRecord

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 4: 验证 `CanonicalRecordTokenCounts` 已正确优先使用真实值

**Files:**
- Verify: `internal/db/canonical_metrics.go`

**Context:** `CanonicalRecordTokenCounts` 已经是从 sidecar metadata 中读取 `prompt_tokens`/`completion_tokens`/`total_tokens`。由于 `persistCanonicalExecutionRecord` 现在会把真实值（或 fallback 估算值）写入 metadata，这个函数无需修改即可工作。

- [ ] **Step 1: 验证现有逻辑**

确认 `internal/db/canonical_metrics.go:12-20`：

```go
func CanonicalRecordTokenCounts(record CanonicalExecutionRecordRow) (prompt, completion, total int) {
	if record.Sidecar == nil {
		return 0, 0, 0
	}
	prompt = canonicalMetadataInt(record.Sidecar.Metadata, "prompt_tokens")
	completion = canonicalMetadataInt(record.Sidecar.Metadata, "completion_tokens")
	total = canonicalMetadataInt(record.Sidecar.Metadata, "total_tokens")
	return prompt, completion, total
}
```

这个逻辑已经正确：优先从 metadata 读取，读不到返回 0。由于 `persistCanonicalExecutionRecord` 总是写入这些字段（真实值或估算值），读取端无需改动。

- [ ] **Step 2: 运行相关测试**

Run: `go test ./internal/db -run TestCanonicalRecordTokenCounts -v`
Expected: PASS（已有测试）

- [ ] **Step 3: Commit（如需要）**

如无改动，无需 commit。

---

### Task 5: 前端删除 Models 页面并添加重定向

**Files:**
- Delete: `frontend/src/pages/Models.tsx`
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: 删除 Models.tsx**

```bash
git rm frontend/src/pages/Models.tsx
```

- [ ] **Step 2: 修改 App.tsx**

在 `frontend/src/App.tsx` 中：
1. 删除 `import { Models } from './pages/Models';`
2. 将 `<Route path="/models" element={<Models />} />` 替换为重定向：

```tsx
<Route path="/models" element={<Navigate to="/policies" replace />} />
```

修改后的路由部分：

```tsx
<Route element={<Layout theme={theme} onToggleTheme={toggleTheme} />}>
  <Route path="/dashboard" element={<Dashboard />} />
  <Route path="/logs" element={<Logs />} />
  <Route path="/logs/:id" element={<LogDetail />} />
  <Route path="/account" element={<Account />} />
  <Route path="/policies" element={<Policies />} />
  <Route path="/models" element={<Navigate to="/policies" replace />} />
  <Route path="/settings" element={<Settings />} />
  <Route path="*" element={<Navigate to="/dashboard" replace />} />
</Route>
```

- [ ] **Step 3: 前端构建确认**

Run: `cd frontend && npm run build`
Expected: 构建成功

- [ ] **Step 4: Commit**

```bash
git add frontend/src/App.tsx
git commit -m "feat(frontend): remove Models page, redirect to Policies

- Delete Models.tsx
- /models route now redirects to /policies

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 6: 后端删除 `/admin/mappings` 相关代码

**Files:**
- Modify: `internal/api/server.go`（删除路由注册）
- Modify: `internal/api/admin_handlers.go`（删除 handlers 和辅助函数）
- Modify: `internal/api/admin_policies_test.go`（删除测试）

- [ ] **Step 1: 删除 server.go 中的路由注册**

在 `internal/api/server.go` 中，删除以下路由注册（约第 141-158 行）：

```go
// 删除以下代码块:
	mux.HandleFunc("/admin/mappings", func(w http.ResponseWriter, r *http.Request) {
		if r.Method == http.MethodGet {
			server.handleAdminMappingsList(w, r)
			return
		}
		server.handleAdminMappingsCreate(w, r)
	})
	mux.HandleFunc("/admin/mappings/", func(w http.ResponseWriter, r *http.Request) {
		if r.URL.Path == "/admin/mappings/test" {
			server.handleAdminMappingsTest(w, r)
			return
		}
		if r.Method == http.MethodPut {
			server.handleAdminMappingsUpdate(w, r)
			return
		}
		server.handleAdminMappingsDelete(w, r)
	})
```

- [ ] **Step 2: 删除 admin_handlers.go 中的 handlers 和辅助函数**

在 `internal/api/admin_handlers.go` 中，删除以下函数（约第 313-555 行）：

```go
// 删除以下所有函数:
// handleAdminMappingsList
// handleAdminMappingsCreate
// handleAdminMappingsUpdate
// handleAdminMappingsDelete
// handleAdminMappingsTest
// compatModelMappings
// compatEnabledModelMappings
// compatModelMappingByID
// policyFromModelMapping
// modelMappingFromPolicy
// modelMappingFromPolicyIfCompatible
```

- [ ] **Step 3: 删除 admin_policies_test.go 中的测试**

删除 `internal/api/admin_policies_test.go` 中所有与 `/admin/mappings` 相关的测试代码（约第 154-299 行）。

- [ ] **Step 4: 编译和测试确认**

Run: `go build ./...`
Run: `go test ./internal/api -run TestAdmin -v`
Expected: 编译通过，无 `/admin/mappings` 相关测试

- [ ] **Step 5: Commit**

```bash
git add internal/api/server.go internal/api/admin_handlers.go internal/api/admin_policies_test.go
git commit -m "chore(api): remove /admin/mappings endpoints and compatibility layer

- Delete /admin/mappings handlers and routing
- Remove policy<->model_mapping conversion helpers
- Frontend now uses /admin/policies exclusively

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 7: 集成验证

- [ ] **Step 1: 完整编译**

Run: `go build ./...`
Expected: 无错误

- [ ] **Step 2: 运行全部测试**

Run: `go test ./...`
Expected: 全部通过

- [ ] **Step 3: 前端构建**

Run: `cd frontend && npm run build`
Expected: 构建成功

- [ ] **Step 4: 最终 Commit**

```bash
git add -A
git commit -m "chore(v1.1): finalize precision improvements

- OpenAI/Anthropic handlers extract upstream usage when available
- Fallback to chars/4 when upstream usage is absent
- Remove legacy Models page and /admin/mappings API
- /models redirects to /policies

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Self-Review Checklist

**Spec coverage:**
- [x] Token 估算优先上游 usage → Task 2, 3
- [x] Fallback 字符/4 → Task 1 (persist 函数内部)
- [x] 前端删除 Models → Task 5
- [x] 后端删除 /admin/mappings → Task 6

**Placeholder scan：**
- [x] 无 TBD/TODO
- [x] 所有代码块完整

**Type 一致性：**
- [x] `persistCanonicalExecutionRecord` 签名在所有调用点一致
- [x] `SSEEvent.Usage` 类型在 proxy 包中定义，api 包中使用
- [x] `proxy.Usage` vs `SSEUsage` 区分清晰
